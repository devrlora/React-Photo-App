// A function that logs out user onclick.
// Boolean? If clicked = true =>
// Log current user out
// Link back to login page

import React, { Component } from 'react'

export class Logout extends Component {
    render() {
        return (
            <div>
                <h1>This is the logout function page. This will link to login page</h1>
            </div>
        )
    }
}

export default Logout
